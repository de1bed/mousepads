# Las variables del backend viven ahora en la raíz del repo: ../.env.example
# Se movieron ahí porque las usan las funciones de /api, no sólo techpad/.
SHOPIFY_CLIENT_ID=your_client_id
SHOPIFY_CLIENT_SECRET=your_client_secret
